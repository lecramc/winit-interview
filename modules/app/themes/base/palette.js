import { error, info, neutral, primary, success, warning } from '@/modules/app/themes/colors'

const palette = {
  neutral,
  primary,
  info,
  success,
  warning,
  error,
}

export default palette
